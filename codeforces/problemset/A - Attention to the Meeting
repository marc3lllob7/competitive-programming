#include "bits/stdc++.h"
using namespace std;

int main (){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int ndir;
    int k;
    int tdis;

    cin >> ndir;
    cin >> k;

    tdis = (k - (ndir - 1))/ndir;
    
    cout << tdis;
    return 0;
}
